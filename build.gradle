plugins {
    id 'pl.allegro.tech.build.axion-release' version '1.14.4' apply true
    id 'maven-publish'
}
repositories {
    mavenCentral()
    maven { url 'https://repo.spring.io/milestone' }
}

subprojects {
    scmVersion {
        repository {
            pushTagsOnly = true
        }
    }
}