plugins {
    id "io.gatling.gradle" version "3.9.0"
}

repositories {
    mavenCentral()
}

subprojects {
    scmVersion {
        tag {
            prefix = project.name
            versionSeparator = '-'
        }
        repository {
            pushTagsOnly = true
        }
    }
}